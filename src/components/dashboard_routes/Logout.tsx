/** @format */

import React from "react";

import styles from "./styles.module.css";

const Logout = () => {
  return (
    <div>
      <h2 className={styles.header}>Logout</h2>
    </div>
  );
};

export default Logout;
